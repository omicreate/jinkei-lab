# プライバシー方針

最終更新: 2026-09-19

陣形ラボは、ソフトテニスの陣形やポジショニングをブラウザ上で検討するためのWebアプリです。

端末の中だけに置く情報と、利用者が明示的に外へ出す情報は分けています。

## 端末内にだけ残るデータ（外部へ送りません）

次のデータは、利用者のブラウザ内（localStorage）にのみ保存されます。開発者のサーバーや集計用の表には送りません。

- 配置メモリ（この端末の3枠。選手配置・前提メモ・保存名）
- 初回ヒントを表示済みかどうかのフラグ
- 到達目安を「ジュニア」にしたかどうか

ブラウザのサイトデータを消す、別端末・別ブラウザで開く、端末を初期化すると、これらは見えなくなります。アプリ内の「削除」は配置メモリのその枠だけを消します。

## 利用者が明示的に外へ出すデータ

- **配置リンク**: コート右下のリンクボタンでコピーしたURLには、今の配置・選択中の選手・前提メモ・ジュニア設定だけが含まれます。流入計測用のパラメータは付けません。リンクを受け取った相手のブラウザで配置が開きます。前提メモに個人名・学校名を書いた状態で送る場合は、公開範囲に注意してください。
- **配置画像**: カメラボタンから共有または保存した画像は、利用者の端末の共有先（LINE等）やファイルとして扱われます。アプリが画像をサーバーへアップロードすることはありません。

配置リンクを作る・画像を共有する操作をするまで、配置データは端末の外へ出ません。

## 流入面の計測（任意・配置は含みません）

他の面から `?src=` 付きのURLで開いたときだけ、次の2つを計測用の Google Apps Script へ1回送ります。

- `src`（どの面からのリンクか）
- `app`（このアプリを示す固定値 `jinkei-lab`）

選手の座標、前提メモ、配置メモリ、個人を特定する情報は送りません。印のない通常アクセスでは何も送りません。同じタブでの二重送信を避けるため、ブラウザの sessionStorage に送信済みフラグだけを残します。

## 個人情報の扱い

氏名、学校名、メールアドレスなどの専用入力欄はありません。

前提メモは自由入力です。個人名・学校名・大会名などを書いた配置リンクや画像をSNS等へ出す場合は、公開範囲に注意してください。

## AI機能について

現時点では、外部AI APIへデータを送る機能はありません。

将来AI機能を追加する場合は、送信されるデータ、保存先、費用、利用規約、同意方法を別途明記してから実装します。
